description: >
  Default executor using Docker.
docker:
  - image: "debian:<< parameters.tag >>"
parameters:
  tag:
    default: "bullseye"
    description: >
      Pick a specific docker image variant:
      https://hub.docker.com/_/debian
    type: string
