description: >
  Default executor using Docker.
docker:
  - image: "centos:<< parameters.tag >>"
parameters:
  tag:
    default: "8.3.2011"
    description: >
      Pick a specific docker image variant:
      https://hub.docker.com/_/centos
    type: string
